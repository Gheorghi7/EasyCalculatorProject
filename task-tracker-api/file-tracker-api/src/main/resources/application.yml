---

server:
  port: 9120

spring:
  db:
    schema: "public"
  datasource:
    url: jdbc:postgresql://localhost/postgres
    username: postgres
    password: Zk.,k.,juf7
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
